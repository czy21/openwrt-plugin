name: Build images

on:
  push:
    paths:
      - config/**/profiles.json
  workflow_dispatch:
    inputs:
      version:
        type: string
        description: "version: v23.05.5"
      targets:
        type: string
        description: "targets: x86/64 rockchip/armv8"

jobs:
  build:
    uses: czy21/share/.github/workflows/openwrt-build-image.yml@main
    with: 
      version: ${{ inputs.version }}
      targets: ${{ inputs.targets }}

    